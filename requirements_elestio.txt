# Essential dependencies for Malaysian Loan Bot on Elestio (2GB VPS)
# Simplified to avoid compilation issues

# Core web framework and HTTP
requests==2.31.0
flask==2.3.3

# Database
psycopg2-binary==2.9.7
redis==4.6.0

# OpenAI API (older stable version)
openai==0.28.1

# Environment and configuration
python-dotenv==1.0.0

# Basic utilities
urllib3==2.0.7
certifi==2023.7.22